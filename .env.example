# Convex (required): get from https://dashboard.convex.dev
VITE_CONVEX_URL=

# Clerk (required for auth): get from https://dashboard.clerk.com
# Copy to .env.local for client-side. Use Convex Dashboard for server-side (see README).
VITE_CLERK_PUBLISHABLE_KEY=
# Optional: custom Clerk Frontend API URL (e.g. for custom domain)
# VITE_CLERK_FRONTEND_API_URL=
